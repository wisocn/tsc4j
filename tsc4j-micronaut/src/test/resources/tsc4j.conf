#
# tsc4j configuration
#

refreshInterval: 5m

sources: [
  {impl: "classpath"}

  {
    impl: "files",
    paths: [
      "/tmp/config/"
    ]
  }
]
# vim:shiftwidth=2 softtabstop=2 expandtab
# EOF
