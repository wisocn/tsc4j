#
# HOCON configuration
#

spring {
  application {
    name: "mySuperFunkyApp"
  }
}

# in-app config
app {
  var3: "overriden in funky/application.conf: "${spring.application.name}
}

# vim:shiftwidth=2 softtabstop=2 expandtab
# EOF